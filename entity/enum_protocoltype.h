#ifndef ENUM_PROTOCOLTYPE
#define ENUM_PROTOCOLTYPE

enum ProtocolType
{
    ProtocolType_ICMP   = 1,
    ProtocolType_TCP    = 6,
    ProtocolType_UDP    = 17,
    ProtocolType_unkown = 255,
};

#endif // ENUM_PROTOCOLTYPE

